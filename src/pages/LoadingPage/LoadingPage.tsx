/** Components */
import CentredTemplate from "../../components/templates/Centred/Centred";

const LoadingPage = () => {
    return (
        <CentredTemplate>
            <h1>Loading...</h1>
        </CentredTemplate>
    );
};

export default LoadingPage;
